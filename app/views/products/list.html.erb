<h1>All Products</h1>

<div class="products">
  <% @products.each do |product| %>
    <%= render partial: 'products/product', locals: { product: product } %>
  <% end %>
  <%= paginate @products %>
</div>